#include <glballistic/Buffer.h>   // your header-only library
#include <GLFW/glfw3.h>
#include <glad/glad.h>
#include <iostream>

int main() {
    // GLFW + GLAD init...

    std::cout << "Hello, World" << std::endl;

    while(true) {}
    return 0;
}
